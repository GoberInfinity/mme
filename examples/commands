#ls ## -la -a
-a
-b д 
>Displays all the elements in the directory

#export CASE_INSENSITIVE=1
>Add a new enviroment variable to path
this should be a very large string
for example if I want to see multitple information

#source ~/.bashrc
>Reload the bash file >

#new
> ls in desc * #

# rustup default 1.42.0
> Default stable version for windows